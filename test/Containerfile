FROM fedora:42

ARG USERNAME=testuser

RUN dnf install -y \
    curl \
    fd-find \
    git \
    procps-ng \
    sudo \
    && dnf clean all

RUN useradd -m -s /bin/bash "${USERNAME}" \
    && echo "${USERNAME} ALL=(ALL) NOPASSWD:ALL" >> /etc/sudoers.d/nopasswd

# Install chezmoi
RUN sh -c "$(curl -fsLS get.chezmoi.io)" -- -b /usr/local/bin

# Copy local source tree as the chezmoi source
COPY --chown=${USERNAME}:${USERNAME} . /home/${USERNAME}/.local/share/chezmoi

USER ${USERNAME}
WORKDIR /home/${USERNAME}

# Initialize and apply dotfiles non-interactively
RUN chezmoi init --apply /home/${USERNAME}/.local/share/chezmoi

# Verify key binaries are available
RUN bash -c 'command -v nvim && command -v rg && command -v fd'
